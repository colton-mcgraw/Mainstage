{
  "name": "cpp",
  "version": "0.1.0",
  "description": "C++ plugin. Provide compilation and linking services for C++ code snippets.",
  "path": "target/release/",
  "abi": "inprocess",
  "entry": "cpp_plugin",
  "kind": "inprocess",
  "functions": [
    {
      "name": "compile",
      "args": [
        { "name": "sources",  "kind": "Array<String>"                   },
        { "name": "flags",    "kind": "Array<String>", "optional": true },
        { "name": "compiler", "kind": "String",        "optional": true }
      ],
      "returns": { "name": "result", "kind": "Object" },
      "description": "Compile sources. Returns { ok: bool, path?: string, error?: string }"
    },
    {
      "name": "link",
      "args": [
        { "name": "objects", "kind": "Array<String>"                   },
        { "name": "flags",   "kind": "Array<String>", "optional": true },
        { "name": "output",  "kind": "String"                           }
      ],
      "returns": { "name": "result", "kind": "Object" },
      "description": "Link object files into executable. Returns { ok: bool, path?: string, error?: string }"
    },
    {
      "name": "list_compilers",
      "args": [],
      "returns": { "name": "compilers", "kind": "Array<Object>" },
      "description": "Detect compilers available on the host and return list of {name, path}."
    }
  ]
}
